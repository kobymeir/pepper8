##teamcity[testSuiteStarted name='{{ report_name }}']
{% for file in files -%}
    {% for code, line, pos, description, severity in file.lines -%}
        {% if severity == 'Error' or severity == 'Flake' -%}
##teamcity[testFailed name='{{ report_name }}: {{ file.path }}' message='line, {{ line }}, col {{ pos }}, {{ description }} ({{ code }})']
        {% else -%}
##teamcity[testStdOut name='{{ report_name }}: {{ file.path }}' out='warning: line, {{ line }}, col {{ pos }}, {{ description }} ({{ code }})']
        {% endif -%}
    {% endfor -%}
{% endfor -%}
##teamcity[testSuiteFinished name='{{ report_name }}']
